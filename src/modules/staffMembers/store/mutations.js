export default {
  /**
   * Seed service editor by passed model.
   *
   * @since [*next-version*]
   *
   * @param state
   * @param model
   */
  setServiceEditorState (state, model) {
    state.one = model
  },
}
