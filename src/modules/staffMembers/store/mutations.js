export default {
  /**
   * Seed staff member editor by passed model.
   *
   * @since [*next-version*]
   *
   * @param state
   * @param model
   */
  setStaffMemberEditorState (state, model) {
    state.one = model
  },
}
